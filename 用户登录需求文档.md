# 用户登录需求文档

## 1. 功能目标

为已注册用户提供安全、稳定的身份认证能力。用户通过账号和密码完成登录后，系统校验身份并建立登录会话，使用户能够访问其权限范围内的功能。

具体目标：

- 支持用户使用账号密码登录。
- 校验账号状态和登录凭证的有效性。
- 登录成功后创建会话并返回认证凭证。
- 对连续失败、禁用账号和异常请求进行风险控制。
- 不向调用方泄露账号是否存在、密码是否正确等敏感信息。

## 2. 使用角色

| 角色 | 使用场景 | 权限范围 |
|---|---|---|
| 普通用户 | 登录系统并使用个人权限内的功能 | 仅访问本人及角色授权的数据和功能 |
| 管理员 | 登录后台执行管理操作 | 访问管理员权限范围内的后台功能 |
| 未登录访客 | 访问登录页、注册页、找回密码页等公开页面 | 不得访问需要身份认证的业务功能 |
| 认证服务 | 接收登录请求并完成身份校验 | 读取认证所需数据、创建和失效会话 |

## 3. 输入参数

### 3.1 页面输入

| 参数 | 类型 | 必填 | 规则 | 说明 |
|---|---|---|---|---|
| 登录账号 | 字符串 | 是 | 账号格式合法，长度 3~64 个字符 | 可为用户名、手机号或邮箱，具体以系统配置为准 |
| 登录密码 | 字符串 | 是 | 长度 8~64 个字符 | 传输过程中必须使用 HTTPS |
| 记住登录 | 布尔值 | 否 | `true` 或 `false` | 是否延长会话有效期，默认 `false` |
| 验证码 | 字符串 | 条件必填 | 按验证码规则校验 | 触发风险控制或系统要求时必填 |
| 客户端信息 | 字符串/对象 | 否 | 长度和格式合法 | 用于安全审计和风险识别，不作为身份凭证 |

### 3.2 接口示例

**请求方式：** `POST /api/auth/login`

```json
{
  "account": "user@example.com",
  "password": "********",
  "rememberMe": false,
  "captcha": "8K3M"
}
```

密码不得以明文写入日志、埋点、错误信息或响应内容。

## 4. 正常流程

1. 用户打开登录页面，输入账号和密码。
2. 用户可选填写验证码，并选择是否记住登录。
3. 客户端进行必填项、格式和长度校验。
4. 客户端通过 HTTPS 提交登录请求。
5. 服务端校验请求来源、参数格式、验证码和登录频率。
6. 服务端根据账号查询用户信息，并校验账号状态。
7. 服务端使用安全的密码哈希校验密码，不得直接比较明文密码。
8. 校验通过后，服务端创建登录会话并生成认证凭证。
9. 服务端记录登录时间、客户端信息和结果等安全审计信息。
10. 服务端返回成功结果，客户端保存认证凭证并跳转至用户原计划访问的页面；若无目标页面，则跳转至首页。

### 4.1 成功返回

HTTP 状态码：`200`

```json
{
  "code": "0",
  "message": "登录成功",
  "data": {
    "accessToken": "eyJ...",
    "tokenType": "Bearer",
    "expiresIn": 7200,
    "user": {
      "id": "10001",
      "account": "user@example.com",
      "name": "张三",
      "roles": ["user"]
    }
  }
}
```

## 5. 异常规则

| 场景 | 处理规则 | 返回提示 |
|---|---|---|
| 账号为空 | 拦截请求，不访问认证服务 | 请输入登录账号 |
| 密码为空 | 拦截请求，不访问认证服务 | 请输入登录密码 |
| 参数格式或长度不合法 | 拒绝请求 | 登录信息格式不正确 |
| 账号或密码错误 | 记录失败次数，使用统一错误提示 | 账号或密码错误 |
| 账号不存在 | 与密码错误返回相同提示，避免账号枚举 | 账号或密码错误 |
| 账号未激活 | 拒绝登录，并引导用户完成激活 | 账号尚未激活 |
| 账号被禁用或锁定 | 拒绝登录，提示联系管理员或等待解锁 | 当前账号暂不可登录 |
| 验证码错误或过期 | 拒绝登录并刷新验证码 | 验证码错误或已过期 |
| 连续登录失败 | 达到阈值后要求验证码或临时锁定 | 登录失败次数过多，请稍后再试 |
| 请求频率过高 | 限流并记录安全事件 | 请求过于频繁，请稍后再试 |
| 会话创建失败 | 不视为登录成功，不返回部分凭证 | 系统繁忙，请稍后再试 |
| 网络或系统异常 | 不泄露内部错误细节，记录服务端日志 | 系统繁忙，请稍后再试 |

异常响应示例：

```json
{
  "code": "AUTH_INVALID_CREDENTIALS",
  "message": "账号或密码错误",
  "data": null
}
```

## 6. 权限要求

- 登录接口仅允许通过 HTTPS 访问。
- 未登录用户只能访问认证相关公开接口，访问受保护资源时必须返回未认证错误。
- 登录成功后，认证凭证必须绑定用户身份、会话和过期时间。
- 用户只能访问其角色授权的功能和数据，前端隐藏菜单不能代替服务端鉴权。
- 管理员登录应执行更严格的安全策略；若系统启用多因素认证，必须完成二次验证后才算登录成功。
- 用户退出登录、修改密码、管理员强制下线或会话过期后，原认证凭证必须失效。
- 登录失败次数、来源 IP、设备标识等安全信息仅限认证服务和授权运维人员查看。

## 7. 返回结果

### 7.1 结果定义

| 结果 | HTTP 状态码 | 说明 |
|---|---:|---|
| 登录成功 | 200 | 返回认证凭证、过期时间和必要的用户基本信息 |
| 参数错误 | 400 | 请求参数缺失或格式不合法 |
| 未认证 | 401 | 访问资源时未提供有效认证凭证，通常不作为登录失败的响应 |
| 账号无权限/被限制 | 403 | 账号已被禁用、锁定或不满足登录条件 |
| 请求过于频繁 | 429 | 触发接口或账号限流 |
| 服务异常 | 500 | 服务端无法完成登录，具体原因写入内部日志 |

### 7.2 返回要求

- 成功响应不得返回密码、密码哈希、验证码、密钥等敏感信息。
- 失败响应应使用稳定的业务错误码，前端根据错误码展示提示。
- 错误提示应避免泄露用户是否存在、认证系统内部结构或数据库信息。
- 返回的用户信息仅包含当前客户端完成业务所需的最小字段。

## 8. 业务限制

- 仅允许已注册且状态正常的用户登录。
- 密码最小长度为 8 位，具体复杂度规则由账号安全策略确定。
- 单个账号连续登录失败 5 次后触发验证码；连续失败 10 次后临时锁定 15 分钟。阈值和时长应支持配置。
- 同一 IP 或设备在短时间内的登录请求必须限流，具体阈值由安全策略确定。
- 普通会话默认有效期为 2 小时；选择“记住登录”时可延长，但最长不得超过 30 天。
- 会话闲置超时后必须重新登录；敏感操作可要求重新认证。
- 同一账号是否允许多端同时登录由系统配置决定；若不允许，新会话建立后应使旧会话失效。
- 登录成功后应更新最后登录时间；登录失败不得更新成功登录时间。
- 所有登录成功、失败、锁定、解锁和退出事件必须保留安全审计记录，日志中不得记录明文密码和完整认证凭证。
- 登录接口不得通过 URL 参数传递密码或验证码。

## 9. 验收要点

- 合法账号和密码可以成功登录并获得有效认证凭证。
- 空参数、非法格式、错误密码均能被正确拦截或提示。
- 不存在账号与错误密码的提示不可用于判断账号是否存在。
- 达到失败阈值后，验证码、限流和临时锁定策略按配置生效。
- 未登录或权限不足的用户无法访问受保护资源。
- 会话过期、退出登录和强制下线后，原认证凭证不可继续使用。
- 日志、响应和前端存储中均不存在明文密码。
