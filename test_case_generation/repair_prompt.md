请根据测试用例质量校验报告修复 `test_cases.json`。

要求：

1. 只修复校验报告指出的问题。
2. 不修改冻结的 `prompt.md`。
3. 不新增需求规则，不引用待补充或暂不可测试的测试点。
4. 不删除已有有效用例，除非校验错误明确要求拆分或移除。
5. 保持 JSON 顶层结构、字段结构和枚举值不变。
6. 修复后重新检查用例编号、四类数量、可执行测试点引用和覆盖表。
7. 只输出合法 JSON，不输出 Markdown、代码围栏、解释或额外文字。

原始测试用例 JSON：

{test_cases_json}

质量校验失败报告：

{validation_report}

质量门禁通过的需求分析 JSON（仅用于核对可执行测试点）：

{analysis_json}
